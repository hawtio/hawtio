<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.0.0"
           xmlns:ext="http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.2.0">

  <cm:property-placeholder persistent-id="hawtio-git">
    <cm:default-properties>
      <cm:property name="hawtio.config.dir" value="./etc"/>
      <cm:property name="hawtio.config.repo" value=""/>
      <cm:property name="hawtio.config.pullOnStartup" value="false"/>
      <cm:property name="hawtio.config.pullBeforeOperation" value="false"/>
      <cm:property name="hawtio.config.pullTimePeriod" value="60000"/>
      <cm:property name="hawtio.config.cloneOnStartup" value="false"/>
      <cm:property name="hawtio.config.remote" value="origin"/>
    </cm:default-properties>
  </cm:property-placeholder>

  <bean id="gitKarafFacade" class="io.hawt.git.GitFacade" init-method="init" destroy-method="destroy" scope="singleton">
    <property name="configDirName" value="${hawtio.config.dir}"/>
    <property name="remoteRepository" value="${hawtio.config.repo}"/>
    <property name="pullOnStartup" value="${hawtio.config.pullOnStartup}"/>
    <property name="pullBeforeOperation" value="${hawtio.config.pullBeforeOperation}"/>
    <property name="pullTimePeriod" value="${hawtio.config.pullTimePeriod}"/>
    <property name="cloneRemoteRepoOnStartup" value="${hawtio.config.cloneOnStartup}"/>
    <property name="remote" value="${hawtio.config.remote}"/>
  </bean>

</blueprint>

