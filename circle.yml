machine:
  node:
    version: 0.10.33
  services:

branches:
  only:
    - master

dependencies:
  pre:
  override:
    - mvn install

test:
  override:
    - mvn verify

general:
  artifacts:
    - '**/target/*.jar'
    - '**/target/*.war'
    - '**/target/*features.xml'

