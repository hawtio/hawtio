import { Logger } from '@hawtio/react'

export const pluginName = 'sample-plugin'
export const pluginTitle = 'Sample Plugin'
export const pluginPath = '/sample-plugin'

export const log = Logger.get(pluginName)
